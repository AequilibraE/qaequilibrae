name: DevelopmentTests

on:  [pull_request]


jobs:
  unittest:
#    needs: linting
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        container: [ 'qgis/qgis:latest', 'qgis/qgis:release-3_34']
    container:
      image: ${{ matrix.container }}
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies for Release
        run: |
          python3 -m venv ./.venv
          source ./.venv/bin/activate
          python -m pip install setuptools
          python -m pip install -r test/requirements-test.txt
          python -m pip install -r ci/requirements-dev.txt
          python ./qaequilibrae/download_extra_packages_class.py

      - name: Run tests
        run: |
          export QT_QPA_PLATFORM=offscreen
          python -m pytest test -v --tb=short --cov=qaequilibrae
